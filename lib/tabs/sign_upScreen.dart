import 'package:flutter/material.dart';

class SignUp extends StatelessWidget {
  static const String routeName = "SigUpScreen";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Sign up Screen"),
      ),
    );
  }
}
